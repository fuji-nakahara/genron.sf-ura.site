<nav class="navbar navbar-expand-sm navbar-dark bg-dark mb-3" id="navbar">
  <div class="container">
    <%= link_to '裏SF創作講座', root_path, class: 'navbar-brand' %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbar-content" aria-controls="navbar-content" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbar-content">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item">
          <%= link_to '課題一覧', kadais_path, class: "nav-link#{current_page?(kadais_path) ? ' active' : ''}" %>
        </li>
      </ul>
      <% if current_user %>
        <div class="navbar-nav">
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbar-user-menu" role="button" data-toggle="dropdown" aria-expanded="false">
              <%= image_tag current_user.image_url, size: '30x30', class: 'ml-1 rounded-circle' %>
            </a>
            <ul class="dropdown-menu dropdown-menu-right" aria-labelledby="navbar-user-menu">
              <li><%= link_to 'ログアウト', logout_path, method: :post, class: 'dropdown-item' %></li>
            </ul>
          </li>
        </div>
      <% else %>
        <%= link_to icon('fab', 'twitter', 'Twitter でログイン'), '/auth/twitter', method: :post, class: 'btn btn-info' %>
      <% end %>
    </div>
  </div>
</nav>
