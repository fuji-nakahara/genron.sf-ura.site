<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title><%= content_for?(:title) ? "#{content_for(:title)} - 裏SF創作講座" : '裏SF創作講座' %></title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_pack_tag 'application' %>
  </head>

  <body>
    <nav class="navbar navbar-expand-md navbar-dark bg-dark mb-4" id="navbar">
      <div class="container">
        <%= link_to '裏SF創作講座', root_path, class: 'navbar-brand' %>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbar-content" aria-controls="navbar-content" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbar-content">
          <ul class="navbar-nav">
            <li class="nav-item">
              <%= link_to '課題', kadais_path, class: "nav-link#{params[:controller] == 'kadais' ? ' active' : ''}" %>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <main class="container" id="main">
      <%= yield %>
    </main>
  </body>
</html>
